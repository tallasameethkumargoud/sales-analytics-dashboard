{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn platform_backend.wsgi:application --bind 0.0.0.0:$PORT --workers 3 --timeout 120",
    "restartPolicyType": "ON_FAILURE"
  }
}
